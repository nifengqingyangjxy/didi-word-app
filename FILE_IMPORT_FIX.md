# 文件导入问题修复说明

## 修复的问题

### 问题1：带点号的词组被漏掉
**症状**：导入Excel文件时，"be angry with sb." 这样带点号的词组被跳过，导致导入的单词数量少于文件中的实际数量。

**原因**：正则表达式 `/^[a-zA-Z]+(?:[-'\s][a-zA-Z]+)*$/` 不支持点号(.)和括号()等符号。

**修复**：
- 修改正则表达式为：`/^[a-zA-Z]+(?:[-'\s.()…][a-zA-Z.…]+)*$/`
- 现在支持以下格式的词组：
  - `be angry with sb.`（带点号）
  - `compare … to …`（带省略号）
  - `from … to …`（省略号）
  - `not only … but also …`（省略号）
  - `get into the habit of`（多个单词）
  - `(试图某事的)观点`（带括号）
  - `don't`（带撇号）
  - `well-known`（带连字符）

### 问题2：导入后单词顺序混乱
**症状**：导入后的单词顺序与文件中的顺序不一致，用户无法快速定位缺失的单词。

**原因**：没有记录导入顺序，数据库查询时按默认顺序返回。

**修复**：
1. 添加 `import_order` 字段到数据库
2. 导入时按顺序记录（从1开始）
3. 预览页面添加排序按钮：
   - **按导入顺序**（默认）：保持文件中的原始顺序
   - **按字母顺序**：A-Z排序，方便查找

---

## 导入规则说明

### Excel文件导入规则

**列布局**：每4列为一组
```
[单词, 翻译, 音标, 词性] [单词, 翻译, 音标, 词性] ...
```

**处理顺序**：
1. **先上后下**：从第一行开始，逐行处理
2. **先左后右**：每行从左到右，每4列为一组
3. **章节识别**：
   - 如果某行只有第一列有内容，且包含"Unit"、"Lesson"、"第...单元"等关键词，识别为章节标题
   - 章节标题后的单词归属于该章节

**示例**：
```
九年级上第六单元                    （章节标题）
deal        协议      [di:l]      n.    exam        考试      [ɪɡˈzæm]     n.
fail        未能及格  [feɪl]      v.    guitar      吉他      [ɡɪˈtɑː]     n.
```

导入顺序：
1. deal
2. exam
3. fail
4. guitar

---

## 测试验证

### 测试用例1：带点号的词组

**测试文件**：包含以下单词的Excel文件
```
be angry with sb.    生某人的气    [bi: ˈæŋɡri wɪð sb.]    成语
at least             至少，起码    [æt li:st]              成语
```

**预期结果**：
- ✅ 两个词组都被正确识别
- ✅ 点号被保留
- ✅ 导入后显示为 "be angry with sb." 和 "at least"

### 测试用例2：导入顺序

**测试文件**：包含33个单词的Excel文件（如截图中的"九年级上第六单元"）

**预期结果**：
- ✅ 所有33个单词都被识别
- ✅ 预览列表中的顺序与文件中一致
- ✅ 第1个单词是 "deal"，第2个是 "exam"，第3个是 "fail"...
- ✅ "be angry with sb." 不会被漏掉

### 测试用例3：排序功能

**操作步骤**：
1. 导入文件后，查看预览列表
2. 默认显示"按导入顺序"
3. 点击"按字母顺序"按钮
4. 单词列表按A-Z排序
5. 点击"按导入顺序"按钮
6. 单词列表恢复原始顺序

**预期结果**：
- ✅ 默认按导入顺序显示
- ✅ 切换到字母顺序后，单词按A-Z排列
- ✅ 切换回导入顺序后，恢复原始顺序

---

## 如何验证修复

### 步骤1：准备测试文件

创建一个Excel文件，包含以下内容：

| 九年级上第六单元 |  |  |  |  |  |  |  |
|---|---|---|---|---|---|---|---|
| deal | 协议 | [di:l] | n. | exam | 考试 | [ɪɡˈzæm] | n. |
| fail | 未能及格 | [feɪl] | v. | guitar | 吉他 | [ɡɪˈtɑː] | n. |
| be angry with sb. | 生某人的气 | [bi: ˈæŋɡri wɪð sb.] | 成语 | at least | 至少 | [æt li:st] | 成语 |

### 步骤2：导入文件

1. 打开应用，进入"单词管理"
2. 点击"文件导入"
3. 选择测试文件
4. 等待解析完成

### 步骤3：检查预览列表

**检查点1：单词数量**
- 应该识别到 6 个单词（deal, exam, fail, guitar, be angry with sb., at least）
- 预览列表标题显示"预览列表 (6 个单词)"

**检查点2：导入顺序**
- 默认显示"按导入顺序"按钮为选中状态
- 单词顺序应该是：
  1. deal
  2. exam
  3. fail
  4. guitar
  5. be angry with sb.
  6. at least

**检查点3：带点号的词组**
- "be angry with sb." 应该被正确识别
- 点号应该被保留
- 翻译应该是"生某人的气"

**检查点4：排序功能**
- 点击"按字母顺序"按钮
- 单词顺序变为：
  1. at least
  2. be angry with sb.
  3. deal
  4. exam
  5. fail
  6. guitar
- 点击"按导入顺序"按钮
- 单词顺序恢复为原始顺序

### 步骤4：确认导入

1. 点击"确认导入"按钮
2. 进入"单词管理"页面
3. 查看导入的单词
4. 确认所有6个单词都已导入

---

## 常见问题

### Q1: 为什么有些单词还是被漏掉？

**可能原因**：
1. 单词包含不支持的特殊字符（如中文、数字等）
2. 单词被识别为词性缩写（如 n., v., adj. 等）
3. Excel单元格格式问题（如合并单元格、隐藏行等）

**解决方案**：
- 检查原文件中的单词格式
- 确保单词列只包含英文字母和支持的符号（-、'、.、()、…、空格）
- 取消合并单元格，显示所有隐藏行

### Q2: 导入顺序还是不对怎么办？

**可能原因**：
1. Excel文件中的单词不是按4列一组排列
2. 有空行或空单元格干扰

**解决方案**：
- 确保Excel文件按标准格式排列（每4列一组）
- 删除多余的空行和空单元格
- 使用"按导入顺序"按钮查看实际导入顺序

### Q3: 如何快速定位缺失的单词？

**方法1：使用导入顺序**
1. 在预览列表中，确保选择"按导入顺序"
2. 对照原文件，逐个检查单词
3. 如果发现缺失，记录位置

**方法2：使用字母顺序**
1. 点击"按字母顺序"按钮
2. 按A-Z顺序快速浏览
3. 对照原文件的单词列表

**方法3：使用搜索功能**
1. 导入后，在"单词管理"页面使用搜索功能
2. 输入可能缺失的单词
3. 如果搜索不到，说明确实缺失

---

## 技术细节

### 数据库Schema变更

```sql
-- 添加导入顺序字段
ALTER TABLE words ADD COLUMN IF NOT EXISTS import_order INTEGER DEFAULT 0;

-- 添加索引
CREATE INDEX IF NOT EXISTS idx_words_import_order ON words(collection_id, import_order);
```

### 正则表达式变更

**修改前**：
```javascript
/^[a-zA-Z]+(?:[-'\s][a-zA-Z]+)*$/
```

**修改后**：
```javascript
/^[a-zA-Z]+(?:[-'\s.()…][a-zA-Z.…]+)*$/
```

**支持的符号**：
- `-`：连字符（如 well-known）
- `'`：撇号（如 don't）
- ` `：空格（如 get into）
- `.`：点号（如 sb.）
- `()`：括号（如 (试图某事的)）
- `…`：省略号（如 compare … to …）

---

## 版本信息

- **修复版本**：v145
- **修复日期**：2026-04-11
- **Git提交**：28a099d

---

**如有问题，请查看用户操作手册或提交Issue。**
