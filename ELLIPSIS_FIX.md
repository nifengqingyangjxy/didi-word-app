# 省略号词组导入问题修复

## 问题描述

**症状**：包含省略号的词组（如"compare … to …"）在导入时被分成两个单词或无法识别。

**原因**：正则表达式不支持省略号字符（…）。

---

## 修复内容

### 1. 添加省略号支持

**修改前的正则表达式**：
```javascript
/^[a-zA-Z]+(?:[-'\s.()][a-zA-Z.]+)*$/
```

**修改后的正则表达式**：
```javascript
/^[a-zA-Z]+(?:[-'\s.()…][a-zA-Z.…]+)*$/
```

**新增支持的字符**：
- `…`：省略号（Unicode字符 U+2026）

### 2. 支持的词组格式

现在可以正确识别以下格式的词组：

#### 基本格式
- `compare … to …`（使用省略号）
- `compare ... to ...`（使用三个点）
- `compare A to B`（使用占位符）
- `compare sth. to sth.`（使用sth.）

#### 其他常见格式
- `be angry with sb.`（带点号）
- `get into the habit of`（多个单词）
- `from … to …`（省略号）
- `between … and …`（省略号）
- `not only … but also …`（省略号）
- `either … or …`（省略号）
- `neither … nor …`（省略号）
- `both … and …`（省略号）
- `prefer … to …`（省略号）
- `would rather … than …`（省略号）

#### 复杂格式
- `(试图某事的)观点`（带括号）
- `well-known`（带连字符）
- `don't`（带撇号）
- `at least`（多个单词）

---

## 测试验证

### 测试用例1：省略号词组

**测试数据**：
```
compare … to …        把...比作...        [kəmˈpeə ... tuː ...]      v.
from … to …            从...到...          [frɒm ... tuː ...]         prep.
not only … but also …  不仅...而且...      [nɒt ˈəʊnli ... bʌt ˈɔːlsəʊ ...]  conj.
```

**预期结果**：
- ✅ 所有3个词组都被正确识别
- ✅ 省略号被保留
- ✅ 不会被分成多个单词

### 测试用例2：混合格式

**测试数据**：
```
compare … to …         把...比作...        [kəmˈpeə ... tuː ...]      v.
compare ... to ...     把...比作...        [kəmˈpeə ... tuː ...]      v.
compare A to B         把A比作B            [kəmˈpeə eɪ tuː biː]       v.
compare sth. to sth.   把某物比作某物      [kəmˈpeə sʌmθɪŋ tuː sʌmθɪŋ]  v.
```

**预期结果**：
- ✅ 所有4种格式都被正确识别
- ✅ 每种格式作为独立的词组
- ✅ 不会重复或遗漏

### 测试用例3：Excel文件导入

**Excel文件内容**：

| 单词 | 翻译 | 音标 | 词性 | 单词 | 翻译 | 音标 | 词性 |
|---|---|---|---|---|---|---|---|
| compare … to … | 把...比作... | [kəmˈpeə ... tuː ...] | v. | from … to … | 从...到... | [frɒm ... tuː ...] | prep. |
| be angry with sb. | 生某人的气 | [bi: ˈæŋɡri wɪð sb.] | 成语 | at least | 至少 | [æt li:st] | 成语 |

**预期结果**：
- ✅ 识别到4个词组
- ✅ 顺序为：compare … to …, from … to …, be angry with sb., at least
- ✅ 所有特殊字符都被保留

---

## 技术细节

### 正则表达式说明

```javascript
/^[a-zA-Z]+(?:[-'\s.()…][a-zA-Z.…]+)*$/
```

**结构分析**：
- `^`：字符串开始
- `[a-zA-Z]+`：至少一个字母
- `(?:...)*`：非捕获组，可重复0次或多次
- `[-'\s.()…]`：分隔符（连字符、撇号、空格、点号、括号、省略号）
- `[a-zA-Z.…]+`：至少一个字母、点号或省略号
- `$`：字符串结束

**支持的字符**：
- `a-zA-Z`：英文字母
- `-`：连字符（hyphen）
- `'`：撇号（apostrophe）
- `\s`：空格（space）
- `.`：点号（period）
- `()`：括号（parentheses）
- `…`：省略号（ellipsis, U+2026）

### Unicode字符

**省略号的两种形式**：
1. **三个点**：`...`（3个ASCII字符，U+002E × 3）
2. **省略号**：`…`（1个Unicode字符，U+2026）

**注意**：
- 正则表达式中的`…`匹配Unicode省略号（U+2026）
- 如果用户输入的是三个点`...`，会被识别为三个点号
- 两种形式都能正确识别，但存储时保持原样

---

## 常见问题

### Q1: 为什么有些词组还是被分成多个单词？

**可能原因**：
1. 使用了不支持的分隔符（如中文逗号、分号等）
2. 单词之间有多个连续空格
3. 包含数字或其他非字母字符

**解决方案**：
- 确保使用支持的分隔符：`-`、`'`、` `、`.`、`()`、`…`
- 清理多余的空格
- 移除数字和特殊字符

### Q2: 三个点（...）和省略号（…）有什么区别？

**区别**：
- **三个点**：`...`是3个独立的点号字符
- **省略号**：`…`是1个Unicode字符

**识别规则**：
- 正则表达式中的`.`匹配点号，`…`匹配省略号
- 两种形式都能被正确识别
- 存储时保持原样，不做转换

**建议**：
- 推荐使用省略号（…），更简洁
- 如果使用三个点（...），确保没有额外空格

### Q3: 如何输入省略号（…）？

**输入方法**：

**Windows**：
- 按住 `Alt` 键，输入 `0133`，松开 `Alt` 键
- 或使用输入法的符号面板

**macOS**：
- 按 `Option` + `;`

**Linux**：
- 按 `Ctrl` + `Shift` + `U`，输入 `2026`，按 `Enter`

**Word/Excel**：
- 输入三个点 `...`，Word会自动转换为省略号 `…`
- 或使用"插入" → "符号" → "省略号"

**复制粘贴**：
- 直接复制这个字符：`…`

---

## 修改的文件

### 1. src/pages/ImportWordsPage.tsx

**修改位置**：
- 第576行：Excel解析正则表达式
- 第413行：文本解析正则表达式（短文本）
- 第423行：文本解析正则表达式（单词验证）
- 第454行：文本解析正则表达式（逐行解析）

**修改内容**：
- 在字符类中添加 `…`
- 更新注释，说明支持省略号

---

## 版本信息

- **修复版本**：v145.1
- **修复日期**：2026-04-11
- **相关Issue**：省略号词组导入问题

---

## 测试清单

### 基本测试
- [ ] 导入包含 `compare … to …` 的Excel文件
- [ ] 确认词组被识别为一个完整的单词
- [ ] 确认省略号被保留

### 格式测试
- [ ] 测试 `compare … to …`（省略号）
- [ ] 测试 `compare ... to ...`（三个点）
- [ ] 测试 `compare A to B`（占位符）
- [ ] 测试 `compare sth. to sth.`（sth.）

### 混合测试
- [ ] 测试包含多种格式的文件
- [ ] 确认所有格式都被正确识别
- [ ] 确认不会重复或遗漏

### 排序测试
- [ ] 导入后查看预览列表
- [ ] 确认"按导入顺序"显示正确
- [ ] 确认"按字母顺序"排序正确

---

**如有问题，请查看 FILE_IMPORT_FIX.md 或提交Issue。**
