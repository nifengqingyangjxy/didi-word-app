# 智能表格列识别功能说明

## 功能概述

新版本实现了智能表格列识别功能，能够自动判断哪列是单词、哪列是音标、哪列是翻译，并**优先使用用户文档中的翻译**，避免不必要的API调用和不准确的翻译。

---

## 核心特性

### 1. 自动列类型识别

系统会自动分析表格的前10行数据，识别每列的类型：

| 列类型 | 识别规则 | 置信度要求 |
|--------|----------|------------|
| **单词列** | 纯英文字母，可包含连字符、撇号 | > 60% |
| **音标列** | 包含音标符号（/、[、]）或特殊字符（ə、ɪ、ʊ等） | > 60% |
| **翻译列** | 包含中文字符 | > 60% |
| **词性列** | 匹配词性缩写（n、v、adj、adv等） | > 50% |

### 2. 优先使用用户翻译

**原有问题**：
- 用户上传了带翻译的表格
- 程序忽略用户翻译，自己去网上匹配
- 匹配到的翻译很长很无用

**新版本解决方案**：
```
如果表格中有翻译列 → 直接使用用户翻译
如果翻译为空 → 才调用API获取翻译
```

**示例**：

**输入表格**：
```
| 单词    | 音标        | 中文意思      |
|---------|-------------|---------------|
| achieve | /ə'tʃi:v/   | 达到；实现    |
| active  | /'æktɪv/    | 积极的；活跃的|
```

**识别结果**：
```
📊 列类型识别结果:
  - 列0: word (100%)
  - 列1: phonetic (100%)
  - 列2: translation (100%)

🎯 使用智能列识别模式
```

**提取结果**：
```
📝 提取单词: achieve, 音标: /ə'tʃi:v/, 翻译: 达到；实现
📝 提取单词: active, 音标: /'æktɪv/, 翻译: 积极的；活跃的
```

**API调用**：
```
✅ 所有单词都有翻译，无需自动补充
```

---

## 支持的表格格式

### 格式1：标准三列（单词 | 音标 | 翻译）

```
| 单词    | 音标        | 中文意思      |
|---------|-------------|---------------|
| achieve | /ə'tʃi:v/   | 达到；实现    |
| active  | /'æktɪv/    | 积极的；活跃的|
```

✅ **自动识别**：列0=单词，列1=音标，列2=翻译

---

### 格式2：不同列顺序（单词 | 翻译 | 音标）

```
| 单词    | 中文意思      | 音标        |
|---------|---------------|-------------|
| achieve | 达到；实现    | /ə'tʃi:v/   |
| active  | 积极的；活跃的| /'æktɪv/    |
```

✅ **自动识别**：列0=单词，列1=翻译，列2=音标

---

### 格式3：带词性（单词 | 音标 | 词性 | 翻译）

```
| 单词    | 音标        | 词性 | 中文意思      |
|---------|-------------|------|---------------|
| achieve | /ə'tʃi:v/   | v.   | 达到；实现    |
| active  | /'æktɪv/    | adj. | 积极的；活跃的|
```

✅ **自动识别**：列0=单词，列1=音标，列2=词性，列3=翻译

---

### 格式4：带序号（序号 | 单词 | 音标 | 翻译）

```
| 序号 | 单词    | 音标        | 中文意思      |
|------|---------|-------------|---------------|
| 1    | achieve | /ə'tʃi:v/   | 达到；实现    |
| 2    | active  | /'æktɪv/    | 积极的；活跃的|
```

✅ **自动识别**：列0=序号（忽略），列1=单词，列2=音标，列3=翻译

---

### 格式5：简化格式（单词 | 翻译）

```
| 单词    | 中文意思      |
|---------|---------------|
| achieve | 达到；实现    |
| active  | 积极的；活跃的|
```

✅ **自动识别**：列0=单词，列1=翻译
⚠️ **音标处理**：调用API获取音标

---

### 格式6：带章节标题

```
Unit 1 - 基础单词

| 单词    | 音标        | 中文意思      |
|---------|-------------|---------------|
| achieve | /ə'tʃi:v/   | 达到；实现    |
| active  | /'æktɪv/    | 积极的；活跃的|

Unit 2 - 进阶单词

| 单词    | 音标        | 中文意思      |
|---------|-------------|---------------|
| balance | /'bæləns/   | 平衡；均衡    |
| brave   | /breɪv/     | 勇敢的        |
```

✅ **自动识别**：
- 章节标题：Unit 1 - 基础单词
- 单词数据：achieve, active
- 章节标题：Unit 2 - 进阶单词
- 单词数据：balance, brave

---

## 识别算法

### 1. 列类型统计

对表格前10行进行采样分析：

```typescript
for (const cell of cells) {
  // 检查是否是音标
  if (/^[\[\]\/]/.test(cell) || /[əɪʊɛɔæʌɑːˈˌ]/.test(cell)) {
    phoneticCount++;
    continue;
  }
  
  // 检查是否包含中文
  if (/[\u4e00-\u9fa5]/.test(cell)) {
    chineseCount++;
    continue;
  }
  
  // 检查是否是词性
  if (isPartOfSpeech(cell)) {
    posCount++;
    continue;
  }
  
  // 检查是否是纯英文单词
  if (/^[a-zA-Z]+[-'\s.()…/,a-zA-Z]*$/.test(cell)) {
    wordCount++;
  }
}
```

### 2. 置信度计算

```typescript
const wordRatio = wordCount / total;
const phoneticRatio = phoneticCount / total;
const chineseRatio = chineseCount / total;
const posRatio = posCount / total;

if (phoneticRatio > 0.6) {
  type = 'phonetic';
  confidence = phoneticRatio;
} else if (chineseRatio > 0.6) {
  type = 'translation';
  confidence = chineseRatio;
} else if (wordRatio > 0.6) {
  type = 'word';
  confidence = wordRatio;
} else if (posRatio > 0.5) {
  type = 'pos';
  confidence = posRatio;
}
```

### 3. 数据提取

```typescript
const wordCol = columnInfos.find(c => c.type === 'word');
const phoneticCol = columnInfos.find(c => c.type === 'phonetic');
const translationCol = columnInfos.find(c => c.type === 'translation');
const posCol = columnInfos.find(c => c.type === 'pos');

const word = row[wordCol.index];
const phonetic = phoneticCol ? row[phoneticCol.index] : null;
const translation = translationCol ? row[translationCol.index] : '';
const partOfSpeech = posCol ? row[posCol.index] : null;
```

---

## 使用示例

### 示例1：标准表格

**输入文件**：`words.xlsx`

```
| 单词    | 音标        | 中文意思      |
|---------|-------------|---------------|
| achieve | /ə'tʃi:v/   | 达到；实现    |
| active  | /'æktɪv/    | 积极的；活跃的|
| advice  | /əd'vaɪs/   | 建议；忠告    |
```

**控制台输出**：

```
📊 列类型识别结果: 列0: word (100%), 列1: phonetic (100%), 列2: translation (100%)
📊 表格列识别结果:
  - 单词列: ✅ 已识别
  - 翻译列: ✅ 已识别
🎯 使用智能列识别模式
📝 提取单词: achieve, 音标: /ə'tʃi:v/, 翻译: 达到；实现
📝 提取单词: active, 音标: /'æktɪv/, 翻译: 积极的；活跃的
📝 提取单词: advice, 音标: /əd'vaɪs/, 翻译: 建议；忠告
✅ 智能识别完成: 3个单词
✅ 所有单词都有翻译，无需自动补充
```

**结果**：
- ✅ 3个单词全部识别
- ✅ 音标全部提取
- ✅ 翻译全部使用用户提供的
- ✅ 无API调用

---

### 示例2：缺少音标的表格

**输入文件**：`words.xlsx`

```
| 单词    | 中文意思      |
|---------|---------------|
| achieve | 达到；实现    |
| active  | 积极的；活跃的|
```

**控制台输出**：

```
📊 列类型识别结果: 列0: word (100%), 列1: translation (100%)
📊 表格列识别结果:
  - 单词列: ✅ 已识别
  - 翻译列: ✅ 已识别
🎯 使用智能列识别模式
📝 提取单词: achieve, 音标: 无, 翻译: 达到；实现
📝 提取单词: active, 音标: 无, 翻译: 积极的；活跃的
✅ 智能识别完成: 2个单词
📝 需要补充 2 / 2 个单词
正在补充缺失的翻译和音标... (2个)
✅ 单词 achieve 返回数据: { phonetic: "/ə'tʃi:v/", ... }
✅ 单词 active 返回数据: { phonetic: "/'æktɪv/", ... }
✅ 自动补充完成
```

**结果**：
- ✅ 2个单词全部识别
- ✅ 翻译全部使用用户提供的
- ⚠️ 音标通过API补充
- ✅ 用户翻译未被覆盖

---

### 示例3：带章节的表格

**输入文件**：`words.xlsx`

```
Unit 1

| 单词    | 音标        | 中文意思      |
|---------|-------------|---------------|
| achieve | /ə'tʃi:v/   | 达到；实现    |
| active  | /'æktɪv/    | 积极的；活跃的|

Unit 2

| 单词    | 音标        | 中文意思      |
|---------|-------------|---------------|
| balance | /'bæləns/   | 平衡；均衡    |
| brave   | /breɪv/     | 勇敢的        |
```

**控制台输出**：

```
📊 列类型识别结果: 列0: word (100%), 列1: phonetic (100%), 列2: translation (100%)
🎯 使用智能列识别模式
⏭️ 跳过标题行: 单词
📖 检测到章节: Unit 1
📝 提取单词: achieve, 音标: /ə'tʃi:v/, 翻译: 达到；实现
📝 提取单词: active, 音标: /'æktɪv/, 翻译: 积极的；活跃的
📖 检测到章节: Unit 2
📝 提取单词: balance, 音标: /'bæləns/, 翻译: 平衡；均衡
📝 提取单词: brave, 音标: /breɪv/, 翻译: 勇敢的
✅ 智能识别完成: 2个章节, 4个单词
```

**结果**：
- ✅ 2个章节全部识别
- ✅ 4个单词全部识别
- ✅ 章节信息保留
- ✅ 翻译全部使用用户提供的

---

## 优势对比

### 旧版本

❌ **问题**：
1. 固定列布局，不够灵活
2. 向后查找翻译（1-3列），可能找错
3. 即使有翻译也会调用API
4. API返回的翻译很长很无用
5. 覆盖用户提供的翻译

**示例**：
```
用户表格: achieve | /ə'tʃi:v/ | 达到；实现
程序处理: achieve | /ə'tʃi:v/ | v. 达到；完成；实现；成功；获得；赢得；达到（某年龄、水平、状况）
```

### 新版本

✅ **优势**：
1. 智能识别列类型，自动适应
2. 准确定位翻译列
3. 优先使用用户翻译
4. 只在必要时调用API
5. 保留用户提供的翻译

**示例**：
```
用户表格: achieve | /ə'tʃi:v/ | 达到；实现
程序处理: achieve | /ə'tʃi:v/ | 达到；实现
```

---

## 向后兼容

如果智能识别失败（置信度不足），系统会自动回退到原有的解析逻辑：

```typescript
if (hasWordColumn) {
  // 使用智能识别模式
  return parseExcelWithColumnDetection(rows, columnInfos);
}

// 回退到原有逻辑
const isStandardFormat = detectStandardFormat(rows);
// ... 原有的解析代码
```

这确保了：
- ✅ 新格式表格使用智能识别
- ✅ 旧格式表格仍然正常工作
- ✅ 不影响现有功能

---

## 测试建议

### 测试用例1：标准三列表格

**输入**：
```
| 单词    | 音标        | 中文意思      |
|---------|-------------|---------------|
| achieve | /ə'tʃi:v/   | 达到；实现    |
```

**预期**：
- ✅ 识别单词列、音标列、翻译列
- ✅ 使用用户翻译"达到；实现"
- ✅ 不调用API

---

### 测试用例2：不同列顺序

**输入**：
```
| 单词    | 中文意思      | 音标        |
|---------|---------------|-------------|
| achieve | 达到；实现    | /ə'tʃi:v/   |
```

**预期**：
- ✅ 正确识别列顺序
- ✅ 使用用户翻译"达到；实现"
- ✅ 不调用API

---

### 测试用例3：缺少音标

**输入**：
```
| 单词    | 中文意思      |
|---------|---------------|
| achieve | 达到；实现    |
```

**预期**：
- ✅ 识别单词列、翻译列
- ✅ 使用用户翻译"达到；实现"
- ⚠️ 调用API获取音标
- ✅ 不覆盖用户翻译

---

### 测试用例4：带章节

**输入**：
```
Unit 1
| 单词    | 音标        | 中文意思      |
|---------|-------------|---------------|
| achieve | /ə'tʃi:v/   | 达到；实现    |
```

**预期**：
- ✅ 识别章节"Unit 1"
- ✅ 识别单词列、音标列、翻译列
- ✅ 使用用户翻译"达到；实现"
- ✅ 保留章节信息

---

## 常见问题

### Q1: 如果表格列顺序不标准怎么办？

**A**: 智能识别会自动分析每列的内容特征，不依赖固定的列顺序。只要列的内容特征明显（如翻译列包含中文），就能正确识别。

---

### Q2: 如果翻译列的置信度不足60%怎么办？

**A**: 系统会回退到原有的解析逻辑，使用向后查找的方式寻找翻译。

---

### Q3: 如果用户提供的翻译有错误怎么办？

**A**: 用户可以在导入后的预览界面手动修改翻译，或者删除翻译后重新调用API获取。

---

### Q4: 如果表格中既有用户翻译又有空翻译怎么办？

**A**: 
- 有翻译的单词：使用用户翻译
- 无翻译的单词：调用API获取翻译

---

### Q5: 如何查看识别结果？

**A**: 打开浏览器控制台（F12），查看日志输出：
```
📊 列类型识别结果: 列0: word (100%), 列1: phonetic (100%), 列2: translation (100%)
🎯 使用智能列识别模式
📝 提取单词: achieve, 音标: /ə'tʃi:v/, 翻译: 达到；实现
```

---

## 总结

新版本的智能表格列识别功能：

✅ **自动适应**：支持多种表格格式和列顺序
✅ **准确识别**：基于内容特征识别列类型
✅ **优先用户**：优先使用用户提供的翻译
✅ **减少调用**：只在必要时调用API
✅ **保留数据**：不覆盖用户提供的数据
✅ **向后兼容**：不影响现有功能

这完全解决了用户反馈的问题：
- ❌ 旧版本：忽略用户翻译，API返回的翻译很长很无用
- ✅ 新版本：优先使用用户翻译，准确简洁
