# 关于微信小程序版本的说明

## 当前应用类型

**DIDI单词助记** 目前是一个 **Web应用（H5应用）**，不是微信小程序。

### Web应用 vs 微信小程序

| 特性 | Web应用（当前） | 微信小程序 |
|------|----------------|-----------|
| **开发技术** | React + TypeScript | 微信小程序框架 |
| **运行环境** | 浏览器 | 微信客户端 |
| **访问方式** | 通过URL在浏览器中打开 | 在微信中搜索或扫码打开 |
| **API支持** | Web标准API | 微信原生API |
| **语音功能** | Web Speech API（微信浏览器不支持） | wx.startRecord等（完全支持） |
| **数据存储** | IndexedDB | wx.setStorage等 |
| **分发方式** | 任何浏览器都可访问 | 仅在微信中使用 |

## 为什么在微信中打开会受限？

### 问题根源

1. **微信内置浏览器的限制**：
   - 微信打开Web应用时使用的是内置浏览器（基于X5内核）
   - 这个浏览器对Web API的支持非常有限
   - 不支持SpeechSynthesis（语音合成）
   - 不支持或不稳定支持SpeechRecognition（语音识别）

2. **Web应用的局限**：
   - Web应用只能使用浏览器提供的标准API
   - 无法调用微信的原生功能
   - 无法使用微信的语音API

### 为什么其他小程序可以使用语音功能？

您看到的那些可以使用语音功能的是**真正的微信小程序**，不是Web应用。

**真正的微信小程序**：
- 使用微信小程序开发框架开发
- 可以调用微信的原生API（wx.startRecord、wx.getRecorderManager等）
- 在微信中有完整的功能支持
- 需要在微信公众平台注册和审核

**Web应用在微信中打开**：
- 只是在微信的浏览器中打开网页
- 无法调用微信的原生API
- 功能受限于浏览器支持

## 解决方案

### 方案1：在标准浏览器中使用（当前推荐）

**优点**：
- 立即可用，无需等待开发
- 功能完整，支持语音播放和识别
- 跨平台，任何设备都可以使用

**操作步骤**：
1. 在微信中点击右上角"..."
2. 选择"在浏览器中打开"
3. 选择Chrome或其他浏览器
4. 在浏览器中使用完整功能

**推荐浏览器**：
- Android: Chrome、Edge
- iOS: Safari、Chrome

### 方案2：开发微信小程序版本（未来计划）

**需要做的工作**：

1. **技术选型**：
   - 使用Taro框架（React转小程序）
   - 或使用uni-app框架（Vue转小程序）
   - 或使用原生微信小程序框架

2. **代码改造**：
   - 重写所有页面组件（适配小程序）
   - 重写语音功能（使用微信API）
   - 重写存储功能（使用微信API）
   - 重写文件导入功能（使用微信API）

3. **微信小程序注册**：
   - 注册微信小程序账号
   - 提交审核（需要1-7天）
   - 通过后才能发布

4. **开发周期**：
   - 预计需要2-4周开发时间
   - 加上审核时间，总共需要3-5周

**优点**：
- 在微信中可以使用完整功能
- 用户体验更好
- 无需跳转到浏览器

**缺点**：
- 开发周期长
- 需要微信审核
- 只能在微信中使用
- 维护两套代码（Web版+小程序版）

### 方案3：混合方案（推荐）

**同时提供两个版本**：

1. **Web版本**（当前）：
   - 在浏览器中使用
   - 功能完整
   - 跨平台

2. **微信小程序版本**（未来开发）：
   - 在微信中使用
   - 功能完整
   - 用户体验好

**数据同步**：
- 通过备份/恢复功能同步数据
- 或者开发云端同步功能

## 当前已实现的解决方案

### 1. 数据持久化问题

**问题**：关闭网页后数据丢失

**解决方案**：
1. **持久化存储授权**：
   - 在"数据管理"页面授权持久化存储
   - 浏览器不会自动清除数据

2. **数据备份与恢复**：
   - 导出数据备份文件（JSON格式）
   - 保存到设备或云盘
   - 需要时从备份文件恢复

3. **自动检测**：
   - 应用启动时自动检测数据
   - 如果有数据，直接加载
   - 如果没有数据，提示导入备份

**使用方法**：
1. 进入"设置" → "数据管理与备份"
2. 点击"授权持久化存储"
3. 定期点击"导出数据备份"
4. 将备份文件保存到安全位置

### 2. 微信浏览器兼容性

**问题**：微信浏览器不支持语音功能

**解决方案**：
1. **自动检测**：
   - 应用自动检测是否在微信中打开
   - 显示友好的提示信息

2. **引导用户**：
   - 提示用户在浏览器中打开
   - 提供详细的操作步骤

3. **功能降级**：
   - 在微信中禁用语音功能
   - 其他功能正常使用

## 数据存储原理

### IndexedDB（当前使用）

**什么是IndexedDB**：
- 浏览器提供的本地数据库
- 数据存储在设备上
- 关闭浏览器后数据不会丢失

**数据存储位置**：
- Android Chrome: `/data/data/com.android.chrome/app_chrome/Default/IndexedDB/`
- iOS Safari: 系统管理的私有目录
- 桌面Chrome: `用户目录/AppData/Local/Google/Chrome/User Data/Default/IndexedDB/`

**为什么会丢失数据**：
1. **隐私模式**：在隐私/无痕模式下，关闭后自动清除
2. **浏览器设置**：设置为关闭时清除所有数据
3. **存储空间不足**：浏览器可能自动清除数据
4. **不同浏览器**：不同浏览器的数据不共享

**如何确保数据不丢失**：
1. 授权持久化存储
2. 定期导出备份
3. 不使用隐私模式
4. 不清除浏览器数据
5. 固定使用一个浏览器

## 推荐使用方式

### 最佳实践

1. **选择合适的浏览器**：
   - Android: 使用Chrome浏览器
   - iOS: 使用Safari浏览器

2. **首次使用**：
   - 在浏览器中打开应用
   - 进入"数据管理"页面
   - 授权持久化存储

3. **日常使用**：
   - 添加到主屏幕（像App一样使用）
   - 定期导出备份（建议每周一次）
   - 将备份文件保存到云盘

4. **更换设备**：
   - 在旧设备上导出备份
   - 在新设备上导入备份
   - 继续学习

### 添加到主屏幕（推荐）

**Android Chrome**：
1. 打开应用
2. 点击右上角"..."
3. 选择"添加到主屏幕"
4. 像App一样使用

**iOS Safari**：
1. 打开应用
2. 点击底部分享按钮
3. 选择"添加到主屏幕"
4. 像App一样使用

**优点**：
- 快速访问
- 全屏体验
- 数据持久化

## 总结

### 当前状态

✅ **已解决**：
- 数据持久化问题（通过授权和备份）
- 微信浏览器兼容性（通过检测和提示）
- 数据备份与恢复（完整的备份系统）

⚠️ **部分解决**：
- 语音功能在微信中不可用（需要在浏览器中使用）

❌ **未解决**：
- 微信小程序版本（需要单独开发）

### 建议

**短期（立即可用）**：
1. 在浏览器中使用应用
2. 授权持久化存储
3. 定期导出备份
4. 添加到主屏幕

**长期（未来计划）**：
1. 开发微信小程序版本
2. 实现云端数据同步
3. 提供更多功能

### 常见问题

**Q: 为什么不能在微信中使用语音功能？**
A: 因为这是Web应用，不是微信小程序。微信浏览器不支持Web语音API。

**Q: 什么时候会有微信小程序版本？**
A: 需要2-4周开发时间，加上审核时间，预计3-5周。

**Q: 数据会丢失吗？**
A: 授权持久化存储并定期备份后，数据不会丢失。

**Q: 可以在不同设备间同步数据吗？**
A: 目前通过备份/恢复功能手动同步。未来可能开发云端同步功能。

**Q: 为什么要在浏览器中打开？**
A: 因为标准浏览器支持完整的Web API，包括语音功能。

## 联系与反馈

如果您有任何问题或建议，欢迎反馈。我们会持续改进应用，提供更好的学习体验。
